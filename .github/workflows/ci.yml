name: Qualidade

on:
  push:
  pull_request:

permissions:
  contents: write

jobs:
  quality:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U test -d test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://test:test@127.0.0.1:5432/test
      AUTH_SECRET: test-only-auth-secret-32-characters
      DEMO_MODE: "true"
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
        with:
          version: 11.16.0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --no-frozen-lockfile
      - run: pnpm audit --prod --audit-level high
      - run: pnpm prisma:generate
      - name: Commit refreshed dependency artifacts
        if: github.ref == 'refs/heads/freebuff/continuidade-crm-security-uiux'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pnpm-lock.yaml src/generated/prisma/internal/class.ts src/generated/prisma/internal/prismaNamespace.ts
          if git diff --cached --quiet; then
            echo "No generated dependency changes to commit."
            exit 0
          fi
          git commit -m "build: refresh prisma 7.10 artifacts [skip ci]"
          git push origin HEAD:${GITHUB_REF_NAME}
      - name: Upload refreshed dependency artifacts
        uses: actions/upload-artifact@v4
        with:
          name: q2-repro-artifacts
          path: |
            pnpm-lock.yaml
            src/generated/prisma
          retention-days: 1
      - run: pnpm db:deploy
      - run: pnpm db:seed
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm test:integration
      - run: pnpm build
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm test:e2e --project=desktop
        env:
          E2E_USE_PRODUCTION: "true"
